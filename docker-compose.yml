
version: '3.8'

services:
  lottery-system:
    build: .
    container_name: annual-lottery-app
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
    networks:
      - lottery-network

networks:
  lottery-network:
    driver: bridge
